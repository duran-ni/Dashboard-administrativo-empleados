import { test, expect } from '@playwright/test';

test.describe('HU-05: Escenarios de filtrado en el navegador', () => {
  test('debería cargar la página correctamente', async ({ page }) => {
    // Aquí programaremos el robot en el siguiente paso
  });
});